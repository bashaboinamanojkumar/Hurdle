# v0 runtime files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.DS_Store
Thumbs.db
desktop.ini
.cursor/
.vscode/
.idea/
Huddle_Build_Detailed.md.docx
_docx_extracted.xml
_plan.txt
.pnpm-store/
*.tsbuildinfo

# Tool output and crash logs, which land in the project root on a failed install or run.
*.log
coverage/
.eslintcache
.turbo/

# Vercel CLI
.vercel

# Supabase CLI scratch space. Migrations are the schema's source of truth and stay tracked.
supabase/.branches/
supabase/.temp/
# Superseded pre-rebuild draft that was never applied and no longer matches the database.
supabase/schema.sql

# Environment files: ignore every variant, keep the checked-in template.
# The negation must stay after the wildcard or it has no effect.
.env*
!.env.example
